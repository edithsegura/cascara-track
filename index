<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>CáscaraTrack — SFS Ingeniería</title>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=DM+Sans:wght@400;600;700;800&display=swap" rel="stylesheet"/>
<style>
*{box-sizing:border-box;margin:0;padding:0}
body{background:#0d1117;color:#ccd6f6;font-family:'DM Sans','Segoe UI',sans-serif;min-height:100vh}
::-webkit-scrollbar{width:6px;height:6px}
::-webkit-scrollbar-track{background:#0d1117}
::-webkit-scrollbar-thumb{background:#2d3561;border-radius:3px}

/* HEADER */
.header{background:linear-gradient(90deg,#0d1117,#1a1f2e 50%,#0d1117);border-bottom:1px solid #2d3561;padding:16px 28px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:12px}
.logo-box{width:38px;height:38px;border-radius:8px;background:linear-gradient(135deg,#F0C040,#E8A020);display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0}
.logo-wrap{display:flex;align-items:center;gap:12px}
.logo-title{font-size:17px;font-weight:800;color:#e8f0fe;letter-spacing:.5px}
.logo-sub{font-size:9px;color:#8892b0;letter-spacing:1.5px;text-transform:uppercase}
.badge-live{background:#2ED57322;border:1px solid #2ED573;border-radius:20px;padding:5px 14px;font-size:11px;color:#2ED573;font-weight:700}
.season{text-align:right}
.season-label{font-size:9px;color:#8892b0}
.season-val{font-size:13px;color:#F0C040;font-weight:700}

/* MAIN */
.main{padding:22px 28px}

/* KPI GRID */
.kpi-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:14px;margin-bottom:28px}
.kpi{background:linear-gradient(135deg,#1a1f2e,#222840);border-radius:12px;padding:18px 20px;position:relative;overflow:hidden;border-left:4px solid #ccc}
.kpi-dot{position:absolute;top:-20px;right:-20px;width:80px;height:80px;border-radius:50%}
.kpi-label{font-size:10px;color:#8892b0;text-transform:uppercase;letter-spacing:1.5px;margin-bottom:8px}
.kpi-value{font-size:30px;font-weight:800;font-family:'DM Mono',monospace}
.kpi-unit{font-size:13px;font-weight:600;margin-left:4px}
.kpi-sub{font-size:11px;color:#8892b0;margin-top:6px}
.kpi-alert{font-size:10px;margin-top:4px;font-weight:700;color:#FF4757}

/* TABS */
.tabs{display:flex;gap:4px;margin-bottom:24px;border-bottom:1px solid #2d3561}
.tab-btn{background:transparent;border:none;border-bottom:2px solid transparent;color:#8892b0;padding:10px 18px;font-size:12px;font-weight:600;cursor:pointer;font-family:inherit;border-radius:6px 6px 0 0;transition:all .2s;position:relative}
.tab-btn.active{background:#1a1f2e;border-bottom-color:#F0C040;color:#e8f0fe}
.tab-btn:hover{color:#ccd6f6}
.badge{background:#FF4757;color:#fff;border-radius:10px;font-size:9px;font-weight:700;padding:1px 5px;margin-left:6px}

/* CARDS */
.card{background:#1a1f2e;border-radius:12px;padding:20px;border:1px solid #2d3561;margin-bottom:20px}
.card-alert{border-color:#FF475733}
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-bottom:20px}
.section-title{display:flex;align-items:center;gap:10px;margin-bottom:16px}
.section-bar{width:4px;height:18px;border-radius:2px}
.section-label{font-size:11px;font-weight:700;color:#ccd6f6;text-transform:uppercase;letter-spacing:2px}
.chart-note{font-size:10px;color:#8892b0;margin-bottom:10px}

/* TRAZABILIDAD */
.finca-row{margin-bottom:16px}
.finca-header{display:flex;justify-content:space-between;margin-bottom:8px;align-items:center}
.finca-name{font-size:12px;font-weight:700;color:#ccd6f6}
.finca-lotes{font-size:10px;color:#8892b0;margin-left:10px}
.finca-pct{font-size:13px;font-weight:800}
.bars3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:6px}
.bar-label{font-size:9px;color:#8892b0;margin-bottom:3px}
.bar-track{background:#0d1117;border-radius:4px;height:8px;overflow:hidden}
.bar-fill{height:100%;border-radius:4px;transition:width .8s ease}
.bar-val{font-size:9px;color:#8892b0;margin-top:2px}

/* CADENA */
.cadena{display:flex;align-items:center;overflow-x:auto;padding:10px 0;gap:0}
.cadena-step{text-align:center;width:90px;flex-shrink:0}
.cadena-icon{width:44px;height:44px;border-radius:50%;margin:0 auto 6px;background:#2ED57322;border:2px solid #2ED573;display:flex;align-items:center;justify-content:center;font-size:18px}
.cadena-label{font-size:8px;font-weight:700;color:#ccd6f6;text-transform:uppercase;letter-spacing:.5px}
.cadena-sub{font-size:9px;color:#8892b0;margin-top:2px}
.cadena-date{font-size:9px;color:#F0C040;margin-top:2px}
.cadena-line{width:20px;height:2px;background:#2ED573;flex-shrink:0}
.cadena-ok{margin-top:16px;padding:10px 14px;background:#2ED57311;border:1px solid #2ED57333;border-radius:8px;font-size:11px;color:#2ED573}

/* ALERTAS */
.alerta-row{display:flex;align-items:flex-start;gap:12px;padding:14px 16px;margin-bottom:8px;border-radius:8px;border-left:4px solid}
.alerta-tipo{border-radius:6px;padding:3px 8px;font-size:9px;font-weight:800;flex-shrink:0;letter-spacing:1px}
.alerta-msg{flex:1;font-size:12px;color:#ccd6f6;line-height:1.5}
.alerta-hora{font-size:10px;color:#8892b0;flex-shrink:0;white-space:nowrap}
.accion-row{display:flex;align-items:center;gap:12px;padding:12px 0;border-bottom:1px solid #2d3561}
.accion-row:last-child{border-bottom:none}
.accion-num{width:26px;height:26px;border-radius:50%;background:#FF475722;border:1px solid #FF4757;display:flex;align-items:center;justify-content:center;font-size:11px;color:#FF4757;font-weight:800;flex-shrink:0}
.accion-txt{flex:1;font-size:12px;color:#ccd6f6;line-height:1.4}
.accion-resp{font-size:10px;color:#8892b0;min-width:110px;text-align:right}
.accion-est{font-size:9px;font-weight:700;padding:2px 8px;border-radius:4px;white-space:nowrap}

/* TABLA */
.tabla-wrap{overflow-x:auto;margin-bottom:20px}
table{width:100%;border-collapse:collapse}
th{padding:10px 12px;text-align:left;font-size:9px;color:#8892b0;text-transform:uppercase;letter-spacing:1px;font-weight:600;white-space:nowrap;border-bottom:1px solid #2d3561}
td{padding:12px;font-size:12px;border-bottom:1px solid #2d356140}
.lote-id{color:#F0C040;font-weight:700;font-family:monospace}
.tag{font-size:10px;font-weight:700;padding:3px 10px;border-radius:4px}

/* METRICAS */
.metrics3{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}
.metric-box{background:#0d1117;border-radius:8px;padding:14px 16px}
.metric-label{font-size:10px;color:#8892b0;margin-bottom:6px;line-height:1.4}
.metric-val{font-size:22px;font-weight:800;font-family:'DM Mono',monospace}

/* FOOTER */
.footer{margin-top:28px;border-top:1px solid #2d3561;padding-top:14px;display:flex;justify-content:space-between;flex-wrap:wrap;gap:8px}
.footer span{font-size:10px;color:#8892b0}

/* HIDDEN */
.tab-content{display:none}
.tab-content.active{display:block}

@media(max-width:640px){
  .grid2{grid-template-columns:1fr}
  .metrics3{grid-template-columns:1fr}
  .main{padding:16px}
}
</style>
</head>
<body>

<!-- HEADER -->
<div class="header">
  <div class="logo-wrap">
    <div class="logo-box">🍋</div>
    <div>
      <div class="logo-title">CáscaraTrack</div>
      <div class="logo-sub">Control de Calidad · Cáscara Deshidratada · NOA</div>
    </div>
  </div>
  <div style="display:flex;align-items:center;gap:20px">
    <div class="season">
      <div class="season-label">TEMPORADA</div>
      <div class="season-val">AGO 2024 — ENE 2025</div>
    </div>
    <div class="badge-live">● DEMO</div>
  </div>
</div>

<div class="main">

<!-- KPIs -->
<div class="kpi-grid">
  <div class="kpi" style="border-left-color:#2ED573">
    <div class="kpi-dot" style="background:#2ED57311"></div>
    <div class="kpi-label">Lotes Aprobados (mes)</div>
    <div><span class="kpi-value" style="color:#e8f0fe">49</span><span class="kpi-unit" style="color:#2ED573">lotes</span></div>
    <div class="kpi-sub"><span style="color:#2ED573">+6%&nbsp;&nbsp;</span>de 55 procesados</div>
  </div>
  <div class="kpi" style="border-left-color:#F0C040">
    <div class="kpi-dot" style="background:#F0C04011"></div>
    <div class="kpi-label">Tasa de Aprobación</div>
    <div><span class="kpi-value" style="color:#e8f0fe">89.1</span><span class="kpi-unit" style="color:#F0C040">%</span></div>
    <div class="kpi-sub"><span style="color:#FF4757">−2.1%&nbsp;&nbsp;</span>meta ≥ 92%</div>
  </div>
  <div class="kpi" style="border-left-color:#3B82F6">
    <div class="kpi-dot" style="background:#3B82F611"></div>
    <div class="kpi-label">Humedad Promedio</div>
    <div><span class="kpi-value" style="color:#e8f0fe">10.8</span><span class="kpi-unit" style="color:#3B82F6">%</span></div>
    <div class="kpi-sub">límite BRC: 12%</div>
  </div>
  <div class="kpi" style="border-left-color:#A78BFA">
    <div class="kpi-dot" style="background:#A78BFA11"></div>
    <div class="kpi-label">Pectina Promedio</div>
    <div><span class="kpi-value" style="color:#e8f0fe">29.6</span><span class="kpi-unit" style="color:#A78BFA">%</span></div>
    <div class="kpi-sub"><span style="color:#2ED573">+1.2%&nbsp;&nbsp;</span>mín. exportación: 25%</div>
  </div>
  <div class="kpi" style="border-left-color:#FF4757">
    <div class="kpi-dot" style="background:#FF475711"></div>
    <div class="kpi-label">Lotes con Alerta</div>
    <div><span class="kpi-value" style="color:#FF4757">2</span><span class="kpi-unit" style="color:#FF4757">lotes</span></div>
    <div class="kpi-sub">requieren acción</div>
    <div class="kpi-alert">⚠ Ver sección Alertas</div>
  </div>
  <div class="kpi" style="border-left-color:#FFA502">
    <div class="kpi-dot" style="background:#FFA50211"></div>
    <div class="kpi-label">Tiempo Liberación Lote</div>
    <div><span class="kpi-value" style="color:#e8f0fe">26.4</span><span class="kpi-unit" style="color:#FFA502">hs</span></div>
    <div class="kpi-sub"><span style="color:#2ED573">−3.1h&nbsp;&nbsp;</span>meta &lt; 24h</div>
  </div>
</div>

<!-- TABS -->
<div class="tabs">
  <button class="tab-btn active" onclick="showTab('calidad',this)">🧪 Calidad</button>
  <button class="tab-btn" onclick="showTab('trazabilidad',this)">🔗 Trazabilidad</button>
  <button class="tab-btn" onclick="showTab('alertas',this)">🚨 Alertas <span class="badge">2</span></button>
  <button class="tab-btn" onclick="showTab('lotes',this)">📦 Lotes</button>
</div>

<!-- TAB CALIDAD -->
<div id="tab-calidad" class="tab-content active">
  <div class="grid2">
    <div class="card">
      <div class="section-title"><div class="section-bar" style="background:#FF4757"></div><span class="section-label">Humedad por Turno (%)</span></div>
      <div class="chart-note">Línea roja = límite BRC 12% · Turno T2-Vie supera el límite</div>
      <canvas id="chartHumedad" height="200"></canvas>
    </div>
    <div class="card">
      <div class="section-title"><div class="section-bar" style="background:#A78BFA"></div><span class="section-label">Tendencia Pectina (%) — 8 semanas</span></div>
      <div class="chart-note">Mínimo exportación UE: 25% · Tendencia alcista positiva</div>
      <canvas id="chartPectina" height="200"></canvas>
    </div>
  </div>
  <div class="card">
    <div class="section-title"><div class="section-bar" style="background:#2ED573"></div><span class="section-label">Lotes Aprobados vs. Rechazados por Mes</span></div>
    <canvas id="chartLotes" height="120"></canvas>
  </div>
</div>

<!-- TAB TRAZABILIDAD -->
<div id="tab-trazabilidad" class="tab-content">
  <div class="card">
    <div class="section-title"><div class="section-bar" style="background:#F0C040"></div><span class="section-label">Cobertura de Trazabilidad por Proveedor (%)</span></div>
    <div class="chart-note" style="margin-bottom:16px">Mínimo exigido BRC Global: 85% · Rojo = acción requerida antes de auditoría</div>
    <div id="fincas-container"></div>
  </div>
  <div class="card">
    <div class="section-title"><div class="section-bar" style="background:#3B82F6"></div><span class="section-label">Cadena de Trazabilidad — LOT-2401 → Alemania</span></div>
    <div class="cadena" id="cadena-container"></div>
    <div class="cadena-ok">✓ Trazabilidad completa — Auditable en &lt; 2 horas · Cumple BRC Global Standard</div>
  </div>
</div>

<!-- TAB ALERTAS -->
<div id="tab-alertas" class="tab-content">
  <div class="card">
    <div class="section-title"><div class="section-bar" style="background:#FF4757"></div><span class="section-label">Centro de Alertas Activas</span></div>
    <div id="alertas-container"></div>
  </div>
  <div class="card card-alert">
    <div class="section-title"><div class="section-bar" style="background:#FF4757"></div><span class="section-label">Plan de Acciones — LOT-2403 (Rechazado)</span></div>
    <div id="acciones-container"></div>
  </div>
</div>

<!-- TAB LOTES -->
<div id="tab-lotes" class="tab-content">
  <div class="card">
    <div class="section-title"><div class="section-bar" style="background:#3B82F6"></div><span class="section-label">Registro de Lotes — Enero 2025</span></div>
    <div class="tabla-wrap">
      <table>
        <thead>
          <tr>
            <th>Lote</th><th>Fecha</th><th>Humedad %</th><th>Pectina %</th>
            <th>Microbiológico</th><th>Agroquímicos</th><th>Destino</th><th>Estado</th>
          </tr>
        </thead>
        <tbody id="tabla-lotes"></tbody>
      </table>
    </div>
    <div class="metrics3">
      <div class="metric-box" style="border:1px solid #3B82F633">
        <div class="metric-label">Tiempo promedio campo → exportación</div>
        <div class="metric-val" style="color:#3B82F6">13 días</div>
      </div>
      <div class="metric-box" style="border:1px solid #FFA50233">
        <div class="metric-label">% lotes con trazabilidad completa</div>
        <div class="metric-val" style="color:#FFA502">81.8%</div>
      </div>
      <div class="metric-box" style="border:1px solid #2ED57333">
        <div class="metric-label">Recall: tiempo de aislamiento lote</div>
        <div class="metric-val" style="color:#2ED573">&lt; 2 hs</div>
      </div>
    </div>
  </div>
</div>

<!-- FOOTER -->
<div class="footer">
  <span>SFS Ingeniería y Servicios S.A.S. · Demo comercial · datos simulados</span>
  <span>BRC Global Standard · SGF · SENASA Res. 21/2025 · Global G.A.P.</span>
</div>

</div><!-- /main -->

<script>
// ── DATOS ──────────────────────────────────────────────────────────────────────
const humedad = [10.2,11.8,9.4,12.4,10.9,11.1,9.8,10.5,11.3,13.1];
const turnos  = ["T1-Lun","T2-Lun","T1-Mar","T2-Mar","T1-Mié","T2-Mié","T1-Jue","T2-Jue","T1-Vie","T2-Vie"];
const pectina = [28.4,29.1,27.8,30.2,31.0,29.5,28.9,32.1];
const semanas = ["S1","S2","S3","S4","S5","S6","S7","S8"];
const meses   = ["Ago","Sep","Oct","Nov","Dic","Ene"];
const aprob   = [42,38,51,47,55,49];
const rechaz  = [3,5,2,4,1,6];

const fincas = [
  {n:"Finca El Ángel",  lotes:18, t:94, a:100, c:100},
  {n:"Coop. COTA",      lotes:32, t:71, a:85,  c:90 },
  {n:"Prod. Zerolo",    lotes:12, t:100,a:100, c:100},
  {n:"Finca Los Nogales",lotes:8, t:50, a:62,  c:75 },
  {n:"Agro San Isidro", lotes:21, t:86, a:95,  c:98 },
];

const cadena = [
  {i:"🌱",l:"CAMPO",s:"Finca El Ángel",d:"28/12"},
  {i:"✂️",l:"COSECHA",s:"Cuadrante B3",d:"02/01"},
  {i:"🏭",l:"EMPAQUE",s:"Recepción + análisis",d:"03/01"},
  {i:"🔥",l:"SECADO",s:"Turno 1 — 6.5hs",d:"05/01"},
  {i:"🧪",l:"LABORATORIO",s:"Fisicoquímico + micro",d:"07/01"},
  {i:"✅",l:"LIBERACIÓN",s:"Aprobado BRC",d:"10/01"},
  {i:"🚢",l:"EXPORTACIÓN",s:"Hamburgo, DE",d:"15/01"},
];

const alertas = [
  {tipo:"CRÍTICO",msg:"LOT-2403 humedad 13.1% — supera límite BRC (12%)",hora:"hace 2h",c:"#FF4757"},
  {tipo:"CRÍTICO",msg:"LOT-2403 residuo clorpirifos > LMR UE — lote retenido",hora:"hace 2h",c:"#FF4757"},
  {tipo:"AVISO",  msg:"Finca Los Nogales: 4 lotes sin registros fitosanitarios",hora:"hace 5h",c:"#FFA502"},
  {tipo:"AVISO",  msg:"LOT-2402 tiempo liberación: 38h — meta < 24h",hora:"hace 8h",c:"#FFA502"},
  {tipo:"INFO",   msg:"Auditoría BRC programada: 15 Feb — 12 lotes pendientes",hora:"hace 1d",c:"#2ED573"},
];

const acciones = [
  {n:1,t:"Retener lote — No despachar hasta resolución",r:"Jefe Calidad",e:"PENDIENTE"},
  {n:2,t:"Enviar muestra a laboratorio externo certificado (INTI / LSQA)",r:"Lab. Interno",e:"EN CURSO"},
  {n:3,t:"Notificar a Finca Los Nogales sobre residuo clorpirifos",r:"Ger. Compras",e:"PENDIENTE"},
  {n:4,t:"Investigar turno T2-Vie — temperatura de secado posible causa",r:"Prod. Industrial",e:"PENDIENTE"},
];

const lotes = [
  {id:"LOT-2401",f:"10/01",h:10.2,p:31.4,m:"OK",a:"OK",   e:"APROBADO",   d:"Alemania"},
  {id:"LOT-2402",f:"11/01",h:11.8,p:29.8,m:"OK",a:"OK",   e:"APROBADO",   d:"Francia"},
  {id:"LOT-2403",f:"12/01",h:13.1,p:26.2,m:"OK",a:"ALERTA",e:"RECHAZADO", d:"—"},
  {id:"LOT-2404",f:"13/01",h:10.9,p:30.5,m:"OK",a:"OK",   e:"APROBADO",   d:"USA"},
  {id:"LOT-2405",f:"14/01",h:11.2,p:28.7,m:"OK",a:"OK",   e:"EN ANÁLISIS",d:"—"},
];

// ── TABS ───────────────────────────────────────────────────────────────────────
function showTab(id, btn) {
  document.querySelectorAll('.tab-content').forEach(el=>el.classList.remove('active'));
  document.querySelectorAll('.tab-btn').forEach(el=>el.classList.remove('active'));
  document.getElementById('tab-'+id).classList.add('active');
  btn.classList.add('active');
}

// ── CHART.JS DEFAULTS ─────────────────────────────────────────────────────────
Chart.defaults.color = '#8892b0';
Chart.defaults.borderColor = '#2d3561';
Chart.defaults.font.family = "'DM Sans', sans-serif";

// ── CHART HUMEDAD ──────────────────────────────────────────────────────────────
new Chart(document.getElementById('chartHumedad'), {
  type: 'bar',
  data: {
    labels: turnos,
    datasets: [{
      label: 'Humedad %',
      data: humedad,
      backgroundColor: humedad.map(v => v > 12 ? '#FF475799' : '#3B82F699'),
      borderColor:     humedad.map(v => v > 12 ? '#FF4757' : '#3B82F6'),
      borderWidth: 1,
      borderRadius: 4,
    }]
  },
  options: {
    responsive: true,
    plugins: {
      legend: { display: false },
      annotation: {},
      tooltip: { callbacks: { label: ctx => ctx.parsed.y + '%' } }
    },
    scales: {
      y: {
        min: 8, max: 14,
        grid: { color: '#2d356166' },
        ticks: { callback: v => v + '%' }
      },
      x: { grid: { display: false } }
    }
  },
  plugins: [{
    id: 'limLine',
    afterDraw(chart) {
      const {ctx, scales: {y, x}} = chart;
      const yPos = y.getPixelForValue(12);
      ctx.save();
      ctx.strokeStyle = '#FF4757';
      ctx.lineWidth = 2;
      ctx.setLineDash([6,4]);
      ctx.beginPath();
      ctx.moveTo(x.left, yPos);
      ctx.lineTo(x.right, yPos);
      ctx.stroke();
      ctx.fillStyle = '#FF4757';
      ctx.font = '9px DM Sans';
      ctx.fillText('LÍM 12%', x.right - 50, yPos - 4);
      ctx.restore();
    }
  }]
});

// ── CHART PECTINA ──────────────────────────────────────────────────────────────
new Chart(document.getElementById('chartPectina'), {
  type: 'line',
  data: {
    labels: semanas,
    datasets: [{
      label: 'Pectina %',
      data: pectina,
      borderColor: '#A78BFA',
      backgroundColor: '#A78BFA22',
      borderWidth: 2.5,
      pointBackgroundColor: '#A78BFA',
      pointRadius: 4,
      tension: 0.3,
      fill: true,
    }]
  },
  options: {
    responsive: true,
    plugins: {
      legend: { display: false },
      tooltip: { callbacks: { label: ctx => ctx.parsed.y + '%' } }
    },
    scales: {
      y: {
        min: 24, max: 34,
        grid: { color: '#2d356166' },
        ticks: { callback: v => v + '%' }
      },
      x: { grid: { display: false } }
    }
  },
  plugins: [{
    id: 'minLine',
    afterDraw(chart) {
      const {ctx, scales: {y, x}} = chart;
      const yPos = y.getPixelForValue(25);
      ctx.save();
      ctx.strokeStyle = '#FFA502';
      ctx.lineWidth = 2;
      ctx.setLineDash([6,4]);
      ctx.beginPath();
      ctx.moveTo(x.left, yPos);
      ctx.lineTo(x.right, yPos);
      ctx.stroke();
      ctx.fillStyle = '#FFA502';
      ctx.font = '9px DM Sans';
      ctx.fillText('MÍN 25%', x.right - 50, yPos - 4);
      ctx.restore();
    }
  }]
});

// ── CHART LOTES ────────────────────────────────────────────────────────────────
new Chart(document.getElementById('chartLotes'), {
  type: 'bar',
  data: {
    labels: meses,
    datasets: [
      { label: 'Aprobados', data: aprob, backgroundColor: '#2ED57399', borderColor: '#2ED573', borderWidth: 1, borderRadius: 4 },
      { label: 'Rechazados', data: rechaz, backgroundColor: '#FF475799', borderColor: '#FF4757', borderWidth: 1, borderRadius: 4 },
    ]
  },
  options: {
    responsive: true,
    plugins: {
      legend: { labels: { color: '#ccd6f6', usePointStyle: true } },
    },
    scales: {
      y: { grid: { color: '#2d356166' } },
      x: { grid: { display: false } }
    }
  }
});

// ── FINCAS ─────────────────────────────────────────────────────────────────────
const fc = document.getElementById('fincas-container');
fincas.forEach(f => {
  const c = f.t>=85?'#2ED573':f.t>=70?'#FFA502':'#FF4757';
  fc.innerHTML += `
  <div class="finca-row">
    <div class="finca-header">
      <div><span class="finca-name">${f.n}</span><span class="finca-lotes">${f.lotes} lotes</span></div>
      <span class="finca-pct" style="color:${c}">${f.t}%</span>
    </div>
    <div class="bars3">
      ${[['Trazabilidad general',f.t],['Registros agroquímicos',f.a],['Registros cosecha',f.c]].map(([l,v])=>`
      <div>
        <div class="bar-label">${l}</div>
        <div class="bar-track"><div class="bar-fill" style="width:${v}%;background:${v>=85?'#2ED573':v>=70?'#FFA502':'#FF4757'}"></div></div>
        <div class="bar-val">${v}%</div>
      </div>`).join('')}
    </div>
  </div>`;
});

// ── CADENA ─────────────────────────────────────────────────────────────────────
const cc = document.getElementById('cadena-container');
cadena.forEach((s,i) => {
  cc.innerHTML += `<div class="cadena-step">
    <div class="cadena-icon">${s.i}</div>
    <div class="cadena-label">${s.l}</div>
    <div class="cadena-sub">${s.s}</div>
    <div class="cadena-date">${s.d}</div>
  </div>`;
  if(i<cadena.length-1) cc.innerHTML += `<div class="cadena-line"></div>`;
});

// ── ALERTAS ────────────────────────────────────────────────────────────────────
const ac = document.getElementById('alertas-container');
alertas.forEach(a => {
  ac.innerHTML += `<div class="alerta-row" style="background:${a.c}10;border-color:${a.c};border-left-color:${a.c}">
    <span class="alerta-tipo" style="background:${a.c}22;border:1px solid ${a.c};color:${a.c}">${a.tipo}</span>
    <span class="alerta-msg">${a.msg}</span>
    <span class="alerta-hora">${a.hora}</span>
  </div>`;
});

// ── ACCIONES ────────────────────────────────────────────────────────────────────
const xc = document.getElementById('acciones-container');
acciones.forEach(a => {
  const ec = a.e==='EN CURSO'?'#FFA502':'#FF4757';
  xc.innerHTML += `<div class="accion-row">
    <div class="accion-num">${a.n}</div>
    <div class="accion-txt">${a.t}</div>
    <div class="accion-resp">${a.r}</div>
    <span class="accion-est" style="color:${ec};background:${ec}22;border:1px solid ${ec}">${a.e}</span>
  </div>`;
});

// ── TABLA LOTES ────────────────────────────────────────────────────────────────
const tb = document.getElementById('tabla-lotes');
lotes.forEach(l => {
  const ec = l.e==='APROBADO'?'#2ED573':l.e==='RECHAZADO'?'#FF4757':'#FFA502';
  tb.innerHTML += `<tr style="background:${l.e==='RECHAZADO'?'#FF475708':'transparent'}">
    <td class="lote-id">${l.id}</td>
    <td style="color:#8892b0">${l.f}</td>
    <td style="color:${l.h>12?'#FF4757':'#2ED573'};font-weight:700">${l.h}%</td>
    <td style="color:${l.p<25?'#FF4757':'#A78BFA'};font-weight:700">${l.p}%</td>
    <td style="color:${l.m==='OK'?'#2ED573':'#FF4757'};font-weight:700">${l.m==='OK'?'✓ OK':'✗ FALLO'}</td>
    <td style="color:${l.a==='OK'?'#2ED573':'#FF4757'};font-weight:700">${l.a==='OK'?'✓ OK':'⚠ ALERTA'}</td>
    <td style="color:#ccd6f6">${l.d}</td>
    <td><span class="tag" style="color:${ec};background:${ec}22;border:1px solid ${ec}">${l.e}</span></td>
  </tr>`;
});
</script>
</body>
</html>
